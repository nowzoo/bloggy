{% extends 'layout.twig' %}

{% block title %}
    {{ post.title }} | {{ site.title }}
{% endblock %}


{% block content %}
    <article>
        <header class="page-header">
            <h1>{{ post.title }}</h1>
        </header>
        {{ post.content | safe }}
    </article>
{% endblock %}